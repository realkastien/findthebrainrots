--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--#selene: allow(shadowing)
-- File generated by Blink v0.18.0 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

Events.Reliable[0] = {}
Queue.Reliable[0] = table.create(256)
Events.Reliable[1] = {}
Queue.Reliable[1] = table.create(256)
Events.Reliable[2] = {}
Queue.Reliable[2] = table.create(256)
Events.Reliable[3] = {}
Queue.Reliable[3] = table.create(256)
Events.Reliable[4] = {}
Queue.Reliable[4] = table.create(256)
Events.Reliable[5] = {}
Queue.Reliable[5] = table.create(256)
Queue.Reliable[6] = table.create(256)
Queue.Reliable[7] = table.create(256)
Queue.Reliable[8] = table.create(256)
Events.Reliable[10] = {}
Queue.Reliable[10] = table.create(256)
Events.Reliable[11] = {}
Queue.Reliable[11] = table.create(256)
Events.Reliable[12] = {}
Queue.Reliable[12] = table.create(256)
Events.Reliable[13] = {}
Queue.Reliable[13] = table.create(256)
Queue.Reliable[14] = table.create(256)
Events.Reliable[16] = {}
Queue.Reliable[16] = table.create(256)
Queue.Reliable[17] = table.create(256)
Events.Reliable[18] = {}
Queue.Reliable[18] = table.create(256)
Events.Reliable[19] = {}
Queue.Reliable[19] = table.create(256)
Events.Reliable[20] = {}
Queue.Reliable[20] = table.create(256)
Events.Reliable[22] = {}
Queue.Reliable[22] = table.create(256)
Events.Reliable[23] = {}
Queue.Reliable[23] = table.create(256)
Queue.Reliable[24] = table.create(256)
Events.Reliable[25] = {}
Queue.Reliable[25] = table.create(256)
Events.Reliable[26] = {}
Queue.Reliable[26] = table.create(256)
Events.Reliable[29] = {}
Queue.Reliable[29] = table.create(256)
Events.Reliable[30] = {}
Queue.Reliable[30] = table.create(256)
Events.Reliable[31] = {}
Queue.Reliable[31] = table.create(256)
Events.Reliable[32] = {}
Queue.Reliable[32] = table.create(256)
Events.Reliable[33] = {}
Queue.Reliable[33] = table.create(256)
Events.Reliable[34] = {}
Queue.Reliable[34] = table.create(256)
Queue.Reliable[35] = table.create(256)
Queue.Unreliable[2] = table.create(256)
export type PlayerRole = { ["role_type"]: string, ["chances_remaining"]: number? }
export type MatchData = { ["match_id"]: string, duration: number, ["investigador_count"]: number, ["brainrot_count"]: number, ["npc_count"]: number }
export type PlayerStats = { ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number }
export type CountdownData = { duration: number, ["match_type"]: string? }
export type TaskData = { ["task_id"]: string, ["task_type"]: string, description: string, ["reward_points"]: number? }
export type BrainrotData = { ["target_player_id"]: string, ["finder_player_id"]: string, position: Vector3? }
export type KillData = { ["killer_id"]: string, ["target_id"]: string, ["target_type"]: string, ["was_correct"]: boolean, position: Vector3? }
export type MatchResult = { ["winning_team"]: string, duration: number, ["final_stats"]: { { ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number } } }
local function ReadEVENT_MatchStarted(): ({ ["match_id"]: string, duration: number, ["investigador_count"]: number, ["brainrot_count"]: number, ["npc_count"]: number })
	-- Read BLOCK: 8 bytes
	local BLOCK_START = Read(8)
	local Value = {} :: any
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value["match_id"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Value.duration = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	Value["investigador_count"] = buffer.readu8(RecieveBuffer, BLOCK_START + 4)
	Value["brainrot_count"] = buffer.readu8(RecieveBuffer, BLOCK_START + 5)
	Value["npc_count"] = buffer.readu16(RecieveBuffer, BLOCK_START + 6)
	return Value
end

local function ReadEVENT_MatchEnded(): ({ ["winning_team"]: string, duration: number, ["final_stats"]: { { ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number } } })
	-- Read BLOCK: 6 bytes
	local BLOCK_START = Read(6)
	local Value = {} :: any
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value["winning_team"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Value.duration = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	-- START ARRAY
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	Value["final_stats"] = table.create(Length)
		-- Read BLOCK: 6 bytes
	local ARRAY_START_1 = Read(6 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		Item_1["player_id"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
		-- Read 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.kills = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 1
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.deaths = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1["npcs_killed"] = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_3)
		-- Read 1
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1["players_found"] = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_4)
		table.insert(Value["final_stats"], Item_1)
	end
	-- END ARRAY
	return Value
end

local function ReadEVENT_MatchCountdownStarted(): ({ duration: number, ["match_type"]: string? })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Value = {} :: any
	Value.duration = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value["match_type"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	end
	return Value
end

local function ReadEVENT_MatchCountdownCancelled(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_MatchPaused(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_MatchResumed(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_PlayerRoleAssigned(): ({ ["role_type"]: string, ["chances_remaining"]: number? })
	-- Read BLOCK: 3 bytes
	local BLOCK_START = Read(3)
	local Value = {} :: any
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value["role_type"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		Value["chances_remaining"] = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
	return Value
end

local function ReadEVENT_PlayerRoleChanged(): ({ ["role_type"]: string, ["chances_remaining"]: number? })
	-- Read BLOCK: 3 bytes
	local BLOCK_START = Read(3)
	local Value = {} :: any
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value["role_type"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		Value["chances_remaining"] = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
	return Value
end

local function ReadEVENT_InvestigadorChancesUpdated(): (number)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

local function WriteEVENT_PlayerKillAttempt(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 9)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

local function ReadEVENT_PlayerKilled(): ({ ["killer_id"]: string, ["target_id"]: string, ["target_type"]: string, ["was_correct"]: boolean, position: Vector3? })
	-- Read BLOCK: 8 bytes
	local BLOCK_START = Read(8)
	local Value = {} :: any
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value["killer_id"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	Value["target_id"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
	Value["target_type"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Value["was_correct"] = (buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 7) == 1 then
		-- Read BLOCK: 12 bytes
		local BLOCK_START = Read(12)
		local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
		local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
		local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
		Value.position = Vector3.new(X, Y, Z)
	end
	return Value
end

local function ReadEVENT_BrainrotFound(): ({ ["target_player_id"]: string, ["finder_player_id"]: string, position: Vector3? })
	-- Read BLOCK: 5 bytes
	local BLOCK_START = Read(5)
	local Value = {} :: any
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value["target_player_id"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	Value["finder_player_id"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 4) == 1 then
		-- Read BLOCK: 12 bytes
		local BLOCK_START = Read(12)
		local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
		local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
		local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
		Value.position = Vector3.new(X, Y, Z)
	end
	return Value
end

local function ReadEVENT_PlayerEliminated(): (string, string)
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	local Value2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value1, Value2
end

local function ReadEVENT_PlayerRevived(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_TaskAssigned(): ({ ["task_id"]: string, ["task_type"]: string, description: string, ["reward_points"]: number? })
	-- Read BLOCK: 7 bytes
	local BLOCK_START = Read(7)
	local Value = {} :: any
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value["task_id"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	Value["task_type"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
	Value.description = buffer.readstring(RecieveBuffer, Read(Length), Length)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		Value["reward_points"] = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	end
	return Value
end

local function WriteEVENT_TaskFinished(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 15)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

local function ReadEVENT_TaskCompleted(): (string, { ["task_id"]: string, ["task_type"]: string, description: string, ["reward_points"]: number? })
	-- Read BLOCK: 9 bytes
	local BLOCK_START = Read(9)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	local Value2 = {} :: any
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	Value2["task_id"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
	Value2["task_type"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 6)
	Value2.description = buffer.readstring(RecieveBuffer, Read(Length), Length)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		Value2["reward_points"] = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	end
	return Value1, Value2
end

local function ReadEVENT_TaskFailed(): (string, string)
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	local Value2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value1, Value2
end

local function ReadEVENT_GameStateChanged(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_PlayerJoinedMatch(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_PlayerLeftMatch(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function WriteEVENT_ChatMessage(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 21)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

local function ReadEVENT_ChatMessageBroadcast(): (string, string)
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	local Value2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value1, Value2
end

local function ReadEVENT_SystemMessage(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_PlayerStatsUpdated(): ({ ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number })
	-- Read BLOCK: 6 bytes
	local BLOCK_START = Read(6)
	local Value = {} :: any
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	Value["player_id"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Value.kills = buffer.readu8(RecieveBuffer, BLOCK_START + 2)
	Value.deaths = buffer.readu8(RecieveBuffer, BLOCK_START + 3)
	Value["npcs_killed"] = buffer.readu8(RecieveBuffer, BLOCK_START + 4)
	Value["players_found"] = buffer.readu8(RecieveBuffer, BLOCK_START + 5)
	return Value
end

local function ReadEVENT_LeaderboardUpdated(): ({ { ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number } })
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	-- START ARRAY
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
	if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
	local Value = table.create(Length)
		-- Read BLOCK: 6 bytes
	local ARRAY_START_1 = Read(6 * Length)
	for Index = 1, Length do
		local Item_1 = {} :: any
		-- Read 2
		local OPERATION_OFFSET_0 = ARRAY_START_1
		ARRAY_START_1 += 2
		Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
		Item_1["player_id"] = buffer.readstring(RecieveBuffer, Read(Length), Length)
		-- Read 1
		local OPERATION_OFFSET_1 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.kills = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_1)
		-- Read 1
		local OPERATION_OFFSET_2 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1.deaths = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_2)
		-- Read 1
		local OPERATION_OFFSET_3 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1["npcs_killed"] = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_3)
		-- Read 1
		local OPERATION_OFFSET_4 = ARRAY_START_1
		ARRAY_START_1 += 1
		Item_1["players_found"] = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_4)
		table.insert(Value, Item_1)
	end
	-- END ARRAY
	return Value
end

local function ReadEVENT_PowerUpSpawned(): (string, Vector3, string)
	-- Read BLOCK: 16 bytes
	local BLOCK_START = Read(16)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	local X = buffer.readf32(RecieveBuffer, BLOCK_START + 2)
	local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 6)
	local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 10)
	local Value2 = Vector3.new(X, Y, Z)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 14)
	local Value3 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value1, Value2, Value3
end

local function WriteEVENT_PowerUpCollected(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 27)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

local function WriteEVENT_PowerUpUsed(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 28)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

local function ReadEVENT_PowerUpActivated(): (string, string)
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	local Value2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value1, Value2
end

local function ReadEVENT_ZoneRestricted(): (string, Vector3, number)
	-- Read BLOCK: 18 bytes
	local BLOCK_START = Read(18)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	local X = buffer.readf32(RecieveBuffer, BLOCK_START + 2)
	local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 6)
	local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 10)
	local Value2 = Vector3.new(X, Y, Z)
	local Value3 = buffer.readf32(RecieveBuffer, BLOCK_START + 14)
	return Value1, Value2, Value3
end

local function ReadEVENT_ZoneUnrestricted(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function WriteEVENT_PlayerEnteredZone(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 0)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

local function WriteEVENT_PlayerExitedZone(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 1)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

local function ReadEVENT_NpcSpawned(): (string, Vector3, string)
	-- Read BLOCK: 16 bytes
	local BLOCK_START = Read(16)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	local X = buffer.readf32(RecieveBuffer, BLOCK_START + 2)
	local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 6)
	local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 10)
	local Value2 = Vector3.new(X, Y, Z)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 14)
	local Value3 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value1, Value2, Value3
end

local function ReadEVENT_NpcDestroyed(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_NpcBehaviorChanged(): (string, string)
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	local Value2 = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value1, Value2
end

local function ReadEVENT_GameError(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_DebugInfo(): (string)
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
	MatchStarted = {
		On = NOOP
	},
	MatchEnded = {
		On = NOOP
	},
	MatchCountdownStarted = {
		On = NOOP
	},
	MatchCountdownCancelled = {
		On = NOOP
	},
	MatchPaused = {
		On = NOOP
	},
	MatchResumed = {
		On = NOOP
	},
	PlayerRoleAssigned = {
		On = NOOP
	},
	PlayerRoleChanged = {
		On = NOOP
	},
	InvestigadorChancesUpdated = {
		On = NOOP
	},
	PlayerKillAttempt = {
		Fire = NOOP
	},
	PlayerKilled = {
		On = NOOP
	},
	BrainrotFound = {
		On = NOOP
	},
	PlayerEliminated = {
		On = NOOP
	},
	PlayerRevived = {
		On = NOOP
	},
	TaskAssigned = {
		On = NOOP
	},
	TaskFinished = {
		Fire = NOOP
	},
	TaskCompleted = {
		On = NOOP
	},
	TaskFailed = {
		On = NOOP
	},
	GameStateChanged = {
		On = NOOP
	},
	PlayerJoinedMatch = {
		On = NOOP
	},
	PlayerLeftMatch = {
		On = NOOP
	},
	ChatMessage = {
		Fire = NOOP
	},
	ChatMessageBroadcast = {
		On = NOOP
	},
	SystemMessage = {
		On = NOOP
	},
	PlayerStatsUpdated = {
		On = NOOP
	},
	LeaderboardUpdated = {
		On = NOOP
	},
	PowerUpSpawned = {
		On = NOOP
	},
	PowerUpCollected = {
		Fire = NOOP
	},
	PowerUpUsed = {
		Fire = NOOP
	},
	PowerUpActivated = {
		On = NOOP
	},
	ZoneRestricted = {
		On = NOOP
	},
	ZoneUnrestricted = {
		On = NOOP
	},
	PlayerEnteredZone = {
		Fire = NOOP
	},
	PlayerExitedZone = {
		Fire = NOOP
	},
	NpcSpawned = {
		On = NOOP
	},
	NpcDestroyed = {
		On = NOOP
	},
	NpcBehaviorChanged = {
		On = NOOP
	},
	GameError = {
		On = NOOP
	},
	DebugInfo = {
		On = NOOP
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsClient() then
    error("Client network module can only be required from the client.")
end

local Reliable: RemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent

local function StepReplication()
    if SendCursor <= 0 then
        return
    end

    local Buffer = buffer.create(SendCursor)
    buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
    Reliable:FireServer(Buffer, SendInstances)

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    table.clear(SendInstances)
end
RunService.Heartbeat:Connect(StepReplication)
Reliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 0 then
			local Value: { ["match_id"]: string, duration: number, ["investigador_count"]: number, ["brainrot_count"]: number, ["npc_count"]: number } = ReadEVENT_MatchStarted()
			if #Events.Reliable[0] > 0 then
				for _, Listener in Events.Reliable[0] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[0] > 256 then
					warn("[Blink]: Event queue of \"MatchStarted\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[0], {Value} :: {any})
			end
		elseif Index == 1 then
			local Value: { ["winning_team"]: string, duration: number, ["final_stats"]: { { ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number } } } = ReadEVENT_MatchEnded()
			if #Events.Reliable[1] > 0 then
				for _, Listener in Events.Reliable[1] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[1] > 256 then
					warn("[Blink]: Event queue of \"MatchEnded\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[1], {Value} :: {any})
			end
		elseif Index == 2 then
			local Value: { duration: number, ["match_type"]: string? } = ReadEVENT_MatchCountdownStarted()
			if #Events.Reliable[2] > 0 then
				for _, Listener in Events.Reliable[2] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[2] > 256 then
					warn("[Blink]: Event queue of \"MatchCountdownStarted\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[2], {Value} :: {any})
			end
		elseif Index == 3 then
			local Value: string = ReadEVENT_MatchCountdownCancelled()
			if #Events.Reliable[3] > 0 then
				for _, Listener in Events.Reliable[3] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[3] > 256 then
					warn("[Blink]: Event queue of \"MatchCountdownCancelled\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[3], {Value} :: {any})
			end
		elseif Index == 4 then
			local Value: string = ReadEVENT_MatchPaused()
			if #Events.Reliable[4] > 0 then
				for _, Listener in Events.Reliable[4] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[4] > 256 then
					warn("[Blink]: Event queue of \"MatchPaused\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[4], {Value} :: {any})
			end
		elseif Index == 5 then
			local Value: string = ReadEVENT_MatchResumed()
			if #Events.Reliable[5] > 0 then
				for _, Listener in Events.Reliable[5] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[5] > 256 then
					warn("[Blink]: Event queue of \"MatchResumed\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[5], {Value} :: {any})
			end
		elseif Index == 6 then
			local Value: { ["role_type"]: string, ["chances_remaining"]: number? } = ReadEVENT_PlayerRoleAssigned()
			if Events.Reliable[6] ~= nil then
				task.spawn(Events.Reliable[6], Value)
			else
				if #Queue.Reliable[6] > 256 then
					warn("[Blink]: Event queue of \"PlayerRoleAssigned\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[6], {Value} :: {any})
			end
		elseif Index == 7 then
			local Value: { ["role_type"]: string, ["chances_remaining"]: number? } = ReadEVENT_PlayerRoleChanged()
			if Events.Reliable[7] ~= nil then
				task.spawn(Events.Reliable[7], Value)
			else
				if #Queue.Reliable[7] > 256 then
					warn("[Blink]: Event queue of \"PlayerRoleChanged\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[7], {Value} :: {any})
			end
		elseif Index == 8 then
			local Value: number = ReadEVENT_InvestigadorChancesUpdated()
			if Events.Reliable[8] ~= nil then
				task.spawn(Events.Reliable[8], Value)
			else
				if #Queue.Reliable[8] > 256 then
					warn("[Blink]: Event queue of \"InvestigadorChancesUpdated\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[8], {Value} :: {any})
			end
		elseif Index == 10 then
			local Value: { ["killer_id"]: string, ["target_id"]: string, ["target_type"]: string, ["was_correct"]: boolean, position: Vector3? } = ReadEVENT_PlayerKilled()
			if #Events.Reliable[10] > 0 then
				for _, Listener in Events.Reliable[10] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[10] > 256 then
					warn("[Blink]: Event queue of \"PlayerKilled\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[10], {Value} :: {any})
			end
		elseif Index == 11 then
			local Value: { ["target_player_id"]: string, ["finder_player_id"]: string, position: Vector3? } = ReadEVENT_BrainrotFound()
			if #Events.Reliable[11] > 0 then
				for _, Listener in Events.Reliable[11] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[11] > 256 then
					warn("[Blink]: Event queue of \"BrainrotFound\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[11], {Value} :: {any})
			end
		elseif Index == 12 then
			local Value1: string, Value2: string = ReadEVENT_PlayerEliminated()
			if #Events.Reliable[12] > 0 then
				for _, Listener in Events.Reliable[12] do
					task.spawn(Listener, Value1, Value2)
				end
			else
				if #Queue.Reliable[12] > 256 then
					warn("[Blink]: Event queue of \"PlayerEliminated\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[12], {Value1, Value2} :: {any})
			end
		elseif Index == 13 then
			local Value: string = ReadEVENT_PlayerRevived()
			if #Events.Reliable[13] > 0 then
				for _, Listener in Events.Reliable[13] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[13] > 256 then
					warn("[Blink]: Event queue of \"PlayerRevived\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[13], {Value} :: {any})
			end
		elseif Index == 14 then
			local Value: { ["task_id"]: string, ["task_type"]: string, description: string, ["reward_points"]: number? } = ReadEVENT_TaskAssigned()
			if Events.Reliable[14] ~= nil then
				task.spawn(Events.Reliable[14], Value)
			else
				if #Queue.Reliable[14] > 256 then
					warn("[Blink]: Event queue of \"TaskAssigned\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[14], {Value} :: {any})
			end
		elseif Index == 16 then
			local Value1: string, Value2: { ["task_id"]: string, ["task_type"]: string, description: string, ["reward_points"]: number? } = ReadEVENT_TaskCompleted()
			if #Events.Reliable[16] > 0 then
				for _, Listener in Events.Reliable[16] do
					task.spawn(Listener, Value1, Value2)
				end
			else
				if #Queue.Reliable[16] > 256 then
					warn("[Blink]: Event queue of \"TaskCompleted\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[16], {Value1, Value2} :: {any})
			end
		elseif Index == 17 then
			local Value1: string, Value2: string = ReadEVENT_TaskFailed()
			if Events.Reliable[17] ~= nil then
				task.spawn(Events.Reliable[17], Value1, Value2)
			else
				if #Queue.Reliable[17] > 256 then
					warn("[Blink]: Event queue of \"TaskFailed\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[17], {Value1, Value2} :: {any})
			end
		elseif Index == 18 then
			local Value: string = ReadEVENT_GameStateChanged()
			if #Events.Reliable[18] > 0 then
				for _, Listener in Events.Reliable[18] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[18] > 256 then
					warn("[Blink]: Event queue of \"GameStateChanged\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[18], {Value} :: {any})
			end
		elseif Index == 19 then
			local Value: string = ReadEVENT_PlayerJoinedMatch()
			if #Events.Reliable[19] > 0 then
				for _, Listener in Events.Reliable[19] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[19] > 256 then
					warn("[Blink]: Event queue of \"PlayerJoinedMatch\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[19], {Value} :: {any})
			end
		elseif Index == 20 then
			local Value: string = ReadEVENT_PlayerLeftMatch()
			if #Events.Reliable[20] > 0 then
				for _, Listener in Events.Reliable[20] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[20] > 256 then
					warn("[Blink]: Event queue of \"PlayerLeftMatch\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[20], {Value} :: {any})
			end
		elseif Index == 22 then
			local Value1: string, Value2: string = ReadEVENT_ChatMessageBroadcast()
			if #Events.Reliable[22] > 0 then
				for _, Listener in Events.Reliable[22] do
					task.spawn(Listener, Value1, Value2)
				end
			else
				if #Queue.Reliable[22] > 256 then
					warn("[Blink]: Event queue of \"ChatMessageBroadcast\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[22], {Value1, Value2} :: {any})
			end
		elseif Index == 23 then
			local Value: string = ReadEVENT_SystemMessage()
			if #Events.Reliable[23] > 0 then
				for _, Listener in Events.Reliable[23] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[23] > 256 then
					warn("[Blink]: Event queue of \"SystemMessage\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[23], {Value} :: {any})
			end
		elseif Index == 24 then
			local Value: { ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number } = ReadEVENT_PlayerStatsUpdated()
			if Events.Reliable[24] ~= nil then
				task.spawn(Events.Reliable[24], Value)
			else
				if #Queue.Reliable[24] > 256 then
					warn("[Blink]: Event queue of \"PlayerStatsUpdated\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[24], {Value} :: {any})
			end
		elseif Index == 25 then
			local Value: { { ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number } } = ReadEVENT_LeaderboardUpdated()
			if #Events.Reliable[25] > 0 then
				for _, Listener in Events.Reliable[25] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[25] > 256 then
					warn("[Blink]: Event queue of \"LeaderboardUpdated\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[25], {Value} :: {any})
			end
		elseif Index == 26 then
			local Value1: string, Value2: Vector3, Value3: string = ReadEVENT_PowerUpSpawned()
			if #Events.Reliable[26] > 0 then
				for _, Listener in Events.Reliable[26] do
					task.spawn(Listener, Value1, Value2, Value3)
				end
			else
				if #Queue.Reliable[26] > 256 then
					warn("[Blink]: Event queue of \"PowerUpSpawned\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[26], {Value1, Value2, Value3} :: {any})
			end
		elseif Index == 29 then
			local Value1: string, Value2: string = ReadEVENT_PowerUpActivated()
			if #Events.Reliable[29] > 0 then
				for _, Listener in Events.Reliable[29] do
					task.spawn(Listener, Value1, Value2)
				end
			else
				if #Queue.Reliable[29] > 256 then
					warn("[Blink]: Event queue of \"PowerUpActivated\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[29], {Value1, Value2} :: {any})
			end
		elseif Index == 30 then
			local Value1: string, Value2: Vector3, Value3: number = ReadEVENT_ZoneRestricted()
			if #Events.Reliable[30] > 0 then
				for _, Listener in Events.Reliable[30] do
					task.spawn(Listener, Value1, Value2, Value3)
				end
			else
				if #Queue.Reliable[30] > 256 then
					warn("[Blink]: Event queue of \"ZoneRestricted\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[30], {Value1, Value2, Value3} :: {any})
			end
		elseif Index == 31 then
			local Value: string = ReadEVENT_ZoneUnrestricted()
			if #Events.Reliable[31] > 0 then
				for _, Listener in Events.Reliable[31] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[31] > 256 then
					warn("[Blink]: Event queue of \"ZoneUnrestricted\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[31], {Value} :: {any})
			end
		elseif Index == 32 then
			local Value1: string, Value2: Vector3, Value3: string = ReadEVENT_NpcSpawned()
			if #Events.Reliable[32] > 0 then
				for _, Listener in Events.Reliable[32] do
					task.spawn(Listener, Value1, Value2, Value3)
				end
			else
				if #Queue.Reliable[32] > 256 then
					warn("[Blink]: Event queue of \"NpcSpawned\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[32], {Value1, Value2, Value3} :: {any})
			end
		elseif Index == 33 then
			local Value: string = ReadEVENT_NpcDestroyed()
			if #Events.Reliable[33] > 0 then
				for _, Listener in Events.Reliable[33] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[33] > 256 then
					warn("[Blink]: Event queue of \"NpcDestroyed\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[33], {Value} :: {any})
			end
		elseif Index == 34 then
			local Value1: string, Value2: string = ReadEVENT_NpcBehaviorChanged()
			if #Events.Reliable[34] > 0 then
				for _, Listener in Events.Reliable[34] do
					task.spawn(Listener, Value1, Value2)
				end
			else
				if #Queue.Reliable[34] > 256 then
					warn("[Blink]: Event queue of \"NpcBehaviorChanged\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[34], {Value1, Value2} :: {any})
			end
		elseif Index == 35 then
			local Value: string = ReadEVENT_GameError()
			if Events.Reliable[35] ~= nil then
				task.spawn(Events.Reliable[35], Value)
			else
				if #Queue.Reliable[35] > 256 then
					warn("[Blink]: Event queue of \"GameError\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[35], {Value} :: {any})
			end
		end
	end
end)
Unreliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 2 then
			local Value: string = ReadEVENT_DebugInfo()
			if Events.Unreliable[2] ~= nil then
				task.spawn(Events.Unreliable[2], Value)
			end
		end
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	MatchStarted = {
		On = function(Listener: (Value: { ["match_id"]: string, duration: number, ["investigador_count"]: number, ["brainrot_count"]: number, ["npc_count"]: number }) -> ()): () -> ()
			table.insert(Events.Reliable[0], Listener)
			for Index, Arguments in Queue.Reliable[0] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[0] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[0], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[0], Index)
				end
			end
		end
	},
	MatchEnded = {
		On = function(Listener: (Value: { ["winning_team"]: string, duration: number, ["final_stats"]: { { ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number } } }) -> ()): () -> ()
			table.insert(Events.Reliable[1], Listener)
			for Index, Arguments in Queue.Reliable[1] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[1] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[1], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[1], Index)
				end
			end
		end
	},
	MatchCountdownStarted = {
		On = function(Listener: (Value: { duration: number, ["match_type"]: string? }) -> ()): () -> ()
			table.insert(Events.Reliable[2], Listener)
			for Index, Arguments in Queue.Reliable[2] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[2] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[2], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[2], Index)
				end
			end
		end
	},
	MatchCountdownCancelled = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[3], Listener)
			for Index, Arguments in Queue.Reliable[3] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[3] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[3], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[3], Index)
				end
			end
		end
	},
	MatchPaused = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[4], Listener)
			for Index, Arguments in Queue.Reliable[4] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[4] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[4], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[4], Index)
				end
			end
		end
	},
	MatchResumed = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[5], Listener)
			for Index, Arguments in Queue.Reliable[5] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[5] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[5], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[5], Index)
				end
			end
		end
	},
	PlayerRoleAssigned = {
		On = function(Listener: (Value: { ["role_type"]: string, ["chances_remaining"]: number? }) -> ()): () -> ()
			Events.Reliable[6] = Listener
			for Index, Arguments in Queue.Reliable[6] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[6] = {}
			return function (): ()
				if Events.Reliable[6] == Listener then
					Events.Reliable[6] = nil
				end
			end
		end
	},
	PlayerRoleChanged = {
		On = function(Listener: (Value: { ["role_type"]: string, ["chances_remaining"]: number? }) -> ()): () -> ()
			Events.Reliable[7] = Listener
			for Index, Arguments in Queue.Reliable[7] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[7] = {}
			return function (): ()
				if Events.Reliable[7] == Listener then
					Events.Reliable[7] = nil
				end
			end
		end
	},
	InvestigadorChancesUpdated = {
		On = function(Listener: (Value: number) -> ()): () -> ()
			Events.Reliable[8] = Listener
			for Index, Arguments in Queue.Reliable[8] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[8] = {}
			return function (): ()
				if Events.Reliable[8] == Listener then
					Events.Reliable[8] = nil
				end
			end
		end
	},
	PlayerKillAttempt = {
		Fire = function(Value: string): ()
			WriteEVENT_PlayerKillAttempt(Value)
		end
	},
	PlayerKilled = {
		On = function(Listener: (Value: { ["killer_id"]: string, ["target_id"]: string, ["target_type"]: string, ["was_correct"]: boolean, position: Vector3? }) -> ()): () -> ()
			table.insert(Events.Reliable[10], Listener)
			for Index, Arguments in Queue.Reliable[10] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[10] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[10], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[10], Index)
				end
			end
		end
	},
	BrainrotFound = {
		On = function(Listener: (Value: { ["target_player_id"]: string, ["finder_player_id"]: string, position: Vector3? }) -> ()): () -> ()
			table.insert(Events.Reliable[11], Listener)
			for Index, Arguments in Queue.Reliable[11] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[11] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[11], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[11], Index)
				end
			end
		end
	},
	PlayerEliminated = {
		On = function(Listener: (Value1: string, Value2: string) -> ()): () -> ()
			table.insert(Events.Reliable[12], Listener)
			for Index, Arguments in Queue.Reliable[12] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[12] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[12], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[12], Index)
				end
			end
		end
	},
	PlayerRevived = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[13], Listener)
			for Index, Arguments in Queue.Reliable[13] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[13] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[13], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[13], Index)
				end
			end
		end
	},
	TaskAssigned = {
		On = function(Listener: (Value: { ["task_id"]: string, ["task_type"]: string, description: string, ["reward_points"]: number? }) -> ()): () -> ()
			Events.Reliable[14] = Listener
			for Index, Arguments in Queue.Reliable[14] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[14] = {}
			return function (): ()
				if Events.Reliable[14] == Listener then
					Events.Reliable[14] = nil
				end
			end
		end
	},
	TaskFinished = {
		Fire = function(Value: string): ()
			WriteEVENT_TaskFinished(Value)
		end
	},
	TaskCompleted = {
		On = function(Listener: (Value1: string, Value2: { ["task_id"]: string, ["task_type"]: string, description: string, ["reward_points"]: number? }) -> ()): () -> ()
			table.insert(Events.Reliable[16], Listener)
			for Index, Arguments in Queue.Reliable[16] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[16] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[16], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[16], Index)
				end
			end
		end
	},
	TaskFailed = {
		On = function(Listener: (Value1: string, Value2: string) -> ()): () -> ()
			Events.Reliable[17] = Listener
			for Index, Arguments in Queue.Reliable[17] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[17] = {}
			return function (): ()
				if Events.Reliable[17] == Listener then
					Events.Reliable[17] = nil
				end
			end
		end
	},
	GameStateChanged = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[18], Listener)
			for Index, Arguments in Queue.Reliable[18] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[18] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[18], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[18], Index)
				end
			end
		end
	},
	PlayerJoinedMatch = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[19], Listener)
			for Index, Arguments in Queue.Reliable[19] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[19] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[19], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[19], Index)
				end
			end
		end
	},
	PlayerLeftMatch = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[20], Listener)
			for Index, Arguments in Queue.Reliable[20] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[20] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[20], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[20], Index)
				end
			end
		end
	},
	ChatMessage = {
		Fire = function(Value: string): ()
			WriteEVENT_ChatMessage(Value)
		end
	},
	ChatMessageBroadcast = {
		On = function(Listener: (Value1: string, Value2: string) -> ()): () -> ()
			table.insert(Events.Reliable[22], Listener)
			for Index, Arguments in Queue.Reliable[22] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[22] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[22], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[22], Index)
				end
			end
		end
	},
	SystemMessage = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[23], Listener)
			for Index, Arguments in Queue.Reliable[23] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[23] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[23], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[23], Index)
				end
			end
		end
	},
	PlayerStatsUpdated = {
		On = function(Listener: (Value: { ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number }) -> ()): () -> ()
			Events.Reliable[24] = Listener
			for Index, Arguments in Queue.Reliable[24] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[24] = {}
			return function (): ()
				if Events.Reliable[24] == Listener then
					Events.Reliable[24] = nil
				end
			end
		end
	},
	LeaderboardUpdated = {
		On = function(Listener: (Value: { { ["player_id"]: string, kills: number, deaths: number, ["npcs_killed"]: number, ["players_found"]: number } }) -> ()): () -> ()
			table.insert(Events.Reliable[25], Listener)
			for Index, Arguments in Queue.Reliable[25] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[25] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[25], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[25], Index)
				end
			end
		end
	},
	PowerUpSpawned = {
		On = function(Listener: (Value1: string, Value2: Vector3, Value3: string) -> ()): () -> ()
			table.insert(Events.Reliable[26], Listener)
			for Index, Arguments in Queue.Reliable[26] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[26] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[26], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[26], Index)
				end
			end
		end
	},
	PowerUpCollected = {
		Fire = function(Value: string): ()
			WriteEVENT_PowerUpCollected(Value)
		end
	},
	PowerUpUsed = {
		Fire = function(Value: string): ()
			WriteEVENT_PowerUpUsed(Value)
		end
	},
	PowerUpActivated = {
		On = function(Listener: (Value1: string, Value2: string) -> ()): () -> ()
			table.insert(Events.Reliable[29], Listener)
			for Index, Arguments in Queue.Reliable[29] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[29] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[29], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[29], Index)
				end
			end
		end
	},
	ZoneRestricted = {
		On = function(Listener: (Value1: string, Value2: Vector3, Value3: number) -> ()): () -> ()
			table.insert(Events.Reliable[30], Listener)
			for Index, Arguments in Queue.Reliable[30] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[30] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[30], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[30], Index)
				end
			end
		end
	},
	ZoneUnrestricted = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[31], Listener)
			for Index, Arguments in Queue.Reliable[31] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[31] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[31], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[31], Index)
				end
			end
		end
	},
	PlayerEnteredZone = {
		Fire = function(Value: string): ()
			local Previous = Save()
			Load()
			WriteEVENT_PlayerEnteredZone(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireServer(Buffer, SendInstances)
			Load(Previous)
		end
	},
	PlayerExitedZone = {
		Fire = function(Value: string): ()
			local Previous = Save()
			Load()
			WriteEVENT_PlayerExitedZone(Value)
			local Buffer = buffer.create(SendCursor)
			buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
			Unreliable:FireServer(Buffer, SendInstances)
			Load(Previous)
		end
	},
	NpcSpawned = {
		On = function(Listener: (Value1: string, Value2: Vector3, Value3: string) -> ()): () -> ()
			table.insert(Events.Reliable[32], Listener)
			for Index, Arguments in Queue.Reliable[32] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[32] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[32], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[32], Index)
				end
			end
		end
	},
	NpcDestroyed = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[33], Listener)
			for Index, Arguments in Queue.Reliable[33] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[33] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[33], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[33], Index)
				end
			end
		end
	},
	NpcBehaviorChanged = {
		On = function(Listener: (Value1: string, Value2: string) -> ()): () -> ()
			table.insert(Events.Reliable[34], Listener)
			for Index, Arguments in Queue.Reliable[34] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[34] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[34], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[34], Index)
				end
			end
		end
	},
	GameError = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			Events.Reliable[35] = Listener
			for Index, Arguments in Queue.Reliable[35] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[35] = {}
			return function (): ()
				if Events.Reliable[35] == Listener then
					Events.Reliable[35] = nil
				end
			end
		end
	},
	DebugInfo = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			Events.Unreliable[2] = Listener
			return function (): ()
				if Events.Unreliable[2] == Listener then
					Events.Unreliable[2] = nil
				end
			end
		end
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL